policy_version: "2026.03.01"
rate_limit:
  per_sender_per_min: 20
cooldown_seconds: 2
duplicate_window: 10
max_message_len: 1200
blocked_patterns:
  - "rm -rf"
  - "DROP TABLE"
  - "sudo reboot"
room:
  command_prefix: "/"
  start_paused: false
